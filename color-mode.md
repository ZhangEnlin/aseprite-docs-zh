# 颜色模式

Aseprite 支持三种不同的颜色模式：

* RGB
* 索引 (Indexed)
* 灰度 (Grayscale)

当你[新建精灵](./new-sprite.md)时，必须选择其中一种颜色模式。本节将详细介绍每种模式的细节。

## RGB

![RGB 模式](./color/rgb-mode.png)

RGB，或称 RGBA，代表 Red（红）- Green（绿）- Blue（蓝）- Alpha（透明度）。每个图像像素都包含这四个分量。这意味着每个像素完全独立于其他像素。

Alpha 分量指定颜色的不透明度：
`Alpha = 255` 表示完全不透明，`Alpha = 0` 表示完全透明。

特别注意：背景图层没有 Alpha 分量，因此它始终是不透明的。

## 索引 (Indexed)

![索引模式](./color/indexed-mode.gif)

在此模式下，每个像素都有一个引用调色板颜色的数字。你的调色板最多可包含 256 种颜色，每个像素指向其中一种颜色（从 0 到 255）。

与 RGBA 不同，如果你修改了调色板颜色，所有引用该颜色的像素都会改变其外观。

索引图像的一个重要概念是，对于透明图层，我们需要一个特殊的索引来充当透明色。通常这个索引是 0，但你可以通过 *精灵 > 属性* 菜单进行更改。

## 灰度 (Grayscale)

![灰度模式](./color/gray-mode.png)

这种模式更类似于 RGBA，只是你只有两个通道：明度 (Value) 和 Alpha。
这里 0 表示黑色，255 表示白色。Alpha 通道的行为与 RGBA 模式中完全相同。

---

**另请参阅**

[颜色](./color.md) |
[新建精灵](./new-sprite.md)