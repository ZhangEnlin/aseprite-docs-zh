# 导出

通常你会使用 *文件 > 保存* 来将你的作品[保存](./save.md)为包含完整信息的 [`.aseprite` 文件](./files.md#aseprite)。然后，可以使用 *文件 > 导出 > 导出为* 菜单选项（快捷键 <kbd>Ctrl + Alt + Shift + S</kbd>，在 macOS 上为 <kbd>⌥ ⇧ ⌘ S</kbd>）为你的游戏、网站、应用程序等导出精灵，将你的精灵转换为 `.gif` 文件或一系列 `.png` 文件。

*文件 > 导出 > 导出为* 会记住你上次使用的选项，以备你想再次保存副本时使用。

## 导出为图像序列

可以通过指定一个带有编号的文件名和静态图像的文件类型，将整个动画保存为一个序列。例如 `frame1.png`，其中 `1` 指定了第一帧的编号，`.png` 是用于保存每一帧的静态文件类型。了解如何[加载图像序列](./open.md#loading-image-sequences)。

如果你指定其他文件名，如 `frame001.png`，文件名将会是 `frame001.png`、`frame002.png`、`frame003.png` 等。

## 仅导出一帧

通过 *文件 > 导出*，可以导出一帧（或一个图层，或一组选定的帧等），只需将 *帧* 字段更改为 *选定的帧*：

![文件 > 导出 > 导出为 > 选定帧](./exporting/file-export-sel-frame.png)

## 导出时自动调整大小

*文件 > 导出* 对话框包含一个特殊的 *调整大小* 字段，因此可以按其他比例保存副本。例如，这对于将你的动画上传到需要更高缩放比例（例如你原始精灵大小的 400%）的社交网络（如 Twitter）非常有用：

![文件 > 导出 > 调整大小](./exporting/file-export-resize.png)

## 其他导出选项

*文件 > 导出* 中还有其他有用的选项：

![文件 > 导出菜单选项](./exporting/file-export.png)

* *动画方向*：可以按正向、反向或往复模式导出你的动画。
* *应用像素比*：如果你的精灵有特殊的像素长宽比（例如 2:1），勾选此选项将在导出时将像素长宽比应用到最终结果。
* *为 Twitter 导出*：调整动画以避免在 Twitter 上因最后一帧延迟无效而出现一些问题。

---

**另请参阅**

[保存](./save.md) |
[精灵表](./sprite-sheet.md) |
[命令行界面](./cli.md)