# 移动工具

你可以使用 <kbd>V</kbd> 键或按住 <kbd>Ctrl</kbd> 键（在 macOS 上为 <kbd>⌘</kbd>）来选择移动工具 ![移动工具图标](./tools/move-tool.png)。

此工具用于移动 [cel](./cel.md) 的 *xy* 位置。

注意：

* 你不能移动背景图层。
* 使用 <kbd>Shift</kbd>（<kbd>⇧</kbd>）键可锁定 x 轴或 y 轴。

## 选择并移动图层

当你使用 <kbd>Ctrl</kbd>（或 <kbd>⌘</kbd>）键并<kbd>左键单击</kbd>时，移动工具会被
选中，并勾选*自动选择图层*选项：

![自动选择选项](./move-tool/autoselect.png)

此选项允许你快速选择并移动图层：

![自动选择预览](./move-tool/autoselect.gif)

## 移动多个 Cel

你可以同时使用时间轴和精灵图编辑器来移动多个 cel：

1. 在时间轴中选择多个 cel，然后
2. 在精灵图编辑器中移动 cel。

![移动多个 cel](./move-tool/move-multiple-cels.gif)