# 首选项

可以通过 *编辑 > 首选项* 菜单或按 <kbd>Ctrl + K</kbd> / <kbd>⌘ K</kbd> 或 <kbd>⌘ ,</kbd> 键来更改 Aseprite 的配置。如果要配置键盘快捷键，则需要通过 *[编辑 > 键盘快捷键](./keyboard-shortcuts.md)* 菜单选项来完成。

Aseprite 的首选项存储在[首选项文件夹](./preferences-folder.md)中的 `aseprite.ini` 文件里，首选项对话框分为几个部分：

## 常规

* **用户界面**：

  * 单窗口模式 ![单窗口界面](./preferences/ui-one-window.png)：菜单无法移出 Aseprite 主菜单栏。

  * 多窗口模式 ![多窗口界面](./preferences/ui-multi-window.png)：菜单可以移出 Aseprite 主菜单栏。

* **浅色** | **深色** 选项：从 v1.3 版本开始，可以快速在 Aseprite 主题的浅色版本和深色版本之间切换。

* **屏幕缩放**：更改 Aseprite 每个窗口中每个像素的大小。

  默认为 200%，这意味着窗口中的每个像素将在屏幕上以 2x2 的正方形显示。

  当使用大型显示器（例如 4K 显示器）时，可以尝试更改为 300% 或 400%。

  - 如果需要让精灵图的每个像素在屏幕上以一个像素点显示，应该将此选项设置为 100%，在这种情况下，你很有可能需要将下一个选项（*UI 元素缩放*）根据需要增加到 200% 或更高。

* **UI 元素缩放**：这是一个额外的缩放比例，仅应用于用户界面的元素（按钮、标签等），但不修改精灵图编辑器的缩放比例。默认为 100%。

* **语言**：用户界面的语言，默认为英语；但可以获取用户贡献的一些 [*翻译*](https://github.com/aseprite/languages) 。

* **显示 Aseprite 菜单栏**（仅在 macOS 上可用）：[Aseprite 菜单栏](./menu-bar.md) 在 Windows 和 Linux 上默认可见：

    ![Aseprite 菜单栏](./menu-bar/aseprite-menubar.png)

  但在 macOS 上默认隐藏，因为在 macOS 中可以使用系统菜单栏：

    ![macOS 菜单栏](./menu-bar/macos-menubar.png)


* **显示 Aseprite 文件对话框**：使用自定义的 Aseprite 文件查看器菜单。
* **启动 Aseprite 时显示主页选项卡**：在 Aseprite 启动时显示主页选项卡。
* **鼠标悬停时展开菜单栏项目**：勾选此选项后，当鼠标悬停在 [Aseprite 菜单栏](./menu-bar.md) 上时，菜单将自动打开。默认情况下此选项是禁用的，因此你必须点击菜单才能打开它。
* **在调色板各条目间绘制分隔线**：在[颜色栏](./color-bar.md)中的每个调色板条目之间绘制一条黑色的 1px 分隔线。
* **与 Aseprite 开发者共享崩溃数据**：当 Aseprite 崩溃（意外关闭）时，系统会询问你是否愿意在未来自动私下与 Aseprite 团队共享一些崩溃信息。Aseprite 默认不会这样做（此选项默认未勾选），因此你必须同意才能共享此信息。更多详情请见 [#2857](https://github.com/aseprite/aseprite/issues/2857) 。

## 数位板

请参阅[数位板](./tablet.md)部分。

## 文件

* **默认扩展名**：选择用于保存/导出操作的默认文件扩展名。
* **最近项目**：在主页和*文件 > 打开最近文件*下拉菜单中显示的最近文件/文件夹项目的数量。
* **恢复文件选项**：这些选项处理保存精灵图恢复数据的时间间隔。

## 颜色

* **Alpha 和不透明度范围**：Alpha/不透明度颜色范围是使用 `0-255` 还是 `0%-100%` 范围。

  ![范围示例](./preferences/ranges.png)
  
* **色彩管理**：请参阅[颜色配置文件](./color-profile.md)部分。

## 提醒

Aseprite 中的一些（不可撤销的）操作会显示带有 *不再显示此提醒* 复选框的警告消息：

![提醒示例](./preferences/show-alert.png)

可以从*编辑 > 首选项 > 提醒*中重新启用这些对话框。

## 编辑器

用于更改[精灵图编辑器](./sprite-editor.md)常规行为的选项：

* **使用滚轮缩放**：默认勾选，使用 [鼠标滚轮](./mouse-wheel.md) 进行缩放（如果取消勾选，垂直滚动将成为鼠标滚轮的默认操作）。
* **双指上下滑动缩放 (macOS)**：此选项仅在 macOS 上可用，如果启用，能够使用 MacBook 触控板上的双指进行缩放。
* **使用滚轮从中心缩放**：如果希望使用鼠标滚轮时从精灵图编辑器的中心进行[缩放](./zoom.md)，而不是从鼠标位置缩放，请勾选此项。
* **使用按键从中心缩放**：当使用按键[进行缩放](./zoom.md)时（如 <kbd>1</kbd>、<kbd>2</kbd>、<kbd>3</kbd> 等），从精灵图的中心缩放，而非从鼠标位置缩放。
* **在精灵图编辑器中显示滚动条**：取消勾选以隐藏所有精灵图编辑器上的滚动条。
* **在编辑器边缘自动滚动**：勾选此选项后，当正在[绘制](./drawing.md)（按住鼠标按钮）且鼠标触碰到精灵图编辑器的边缘时，它将自动滚动。
* **打开精灵图时自动适应屏幕**：勾选后，当创建一个新精灵图或打开一个现有的精灵图时，编辑器将显示整个图像，使其适应画布的可用空间。当此选项未勾选（默认选项）时，初始缩放级别将默认为 100%。
* **铅笔工具立即预览直线**：当使用 ![铅笔图标](./tools/pencil-tool.png) 铅笔工具时，按下 <kbd>Shift</kbd> 键将从上一个绘制点绘制一条直线。勾选此选项后，按下 <kbd>Shift</kbd> 键后将立即看到线条预览（而不是等待按下鼠标按钮)。
* **使用吸管时丢弃自定义画笔**：如果使用 *编辑 > 新建画笔* 选项创建了一个自定义画笔，使用吸管 ![吸管工具图标](./tools/eyedropper-tool.png) 工具将更改画笔的颜色。勾选此选项后，在使用吸管选取颜色后，自定义画笔将被完全丢弃，并选择一个带有该选取颜色的常规画笔。
* **降采样**：当缩放级别 <= 50% 时，用于显示精灵图的算法。默认情况下为*双线性 mipmapping*。
* **右键单击**：可以自定义 [右键单击](./right-click.md) 要执行的具体操作。

## 选择

* **自动调整不透明/透明模式**：在图层上移动时自动调整不透明/透明模式，例如：移动到透明图层上会将模式设置为透明。
* **“编辑 > 删除”命令后保留选区**：删除内容后保留选区边界。

  ![保留选区示例](./preferences/keep-selection-deleted-content.gif)

* **修改选区时自动显示选区边缘**：当移动选区时，自动启用*视图 > 显示 > 选区边缘*。这将覆盖菜单设置（例如：如果*视图 > 显示 > 选区边缘*已禁用，此选项将启用它）。
* **允许移动选区边缘**：启用此选项允许移动选区边界。只有当鼠标悬停在选区边缘（虚线）上时，才可以移动选区。

  ![移动选区边缘示例](./preferences/move-selection-edges.gif)

* **按下修饰键时禁用变换手柄**：按下修饰键（<kbd>Shift</kbd>/<kbd>Ctrl</kbd>/<kbd>Alt</kbd>）时禁用变换手柄。
* **在添加模式下移动选区**：启用在[添加到选区模式](./selecting.md#Add/Subtract/Intersect)下移动选区。
* **双击选择网格瓦片**：启用通过双击来选择网格瓦片。
* **选项启用时吸附到网格**：当*视图 > 网格 > 吸附到网格*启用时，执行吸附到网格操作。
* **即使对直角/平角也强制使用 RotSprite**：对直角/平角使用 RotSprite 旋转算法。
* **变换时间轴上选中图层或帧中的 Cel**：选区变换将应用于所有选中图层/帧的 cel。如果禁用此选项，选区变换将仅应用于所有选中的 cel。

## 时间轴

* **自动显示时间轴**：当添加新帧或图层时显示时间轴。
* **停止时倒回**：停止播放时，当前帧重置为动画开始播放时所在的帧（注意，该帧可能不是第一帧）。
* **默认第一帧**：设置默认的第一帧。
* **保留选区**：当编辑画布或时间轴失去焦点时，保留时间轴选区范围。
* **点击时选择**：点击时选择帧/cel。
* **Shift + 点击时选择**：使用 <kbd>Shift + 点击</kbd> 时选择帧/cel。
* **拖拽时选择**：拖拽时选择帧/cel。
* **从边缘拖放**：启用从帧/cel 边缘的拖放操作。

## 光标

* **使用原生鼠标光标**：使用操作系统的光标。如果禁用，则使用自定义主题光标。
* **鼠标光标缩放**：主题光标的缩放比例。
* **十字准线类型**：更改绘制时的十字准线类型。

  ![十字准线类型](./preferences/crosshair-types.png)

* **画笔预览**：更改绘制时的画笔预览类型。

  ![画笔预览类型](./preferences/brush-preview-types.gif) 

* **十字准线和画笔边缘颜色**：更改十字准线和画笔预览边缘的边缘颜色。

  ![黑白反转色](./preferences/edge-negative.png)  ![特定颜色](./preferences/edge-color.png)

## 背景

**新建文档的背景**：编辑所有新建文档的默认背景。

**当前活动文档的背景**：编辑当前文档的背景。

* **大小**：设置背景方块的大小。
* **颜色**：设置背景方块的颜色。

## 网格

**新建文档的网格**：编辑所有新建文档的默认网格。

**当前活动文档的网格**：编辑当前文档的网格。

* **X 和 Y**：X 和 Y 轴网格偏移。
* **颜色**：网格线颜色。
* **不透明度**：网格线不透明度。
* **可见像素网格**：启用/禁用像素网格。
  * **颜色**：像素网格线颜色。
  * **不透明度**：像素网格线不透明度。

## 参考线和切片

* **图层边缘颜色**：图层边缘参考线颜色。
* **自动参考线颜色**：自动参考线颜色。

  ![参考线和边缘示例](./preferences/auto-guides.png)

* **默认颜色**：默认切片边缘颜色。

## 撤销

* **撤销限制**：每个精灵图的撤销信息的内存限制，以兆字节为单位。
* **转到修改的帧/图层**：将当前帧/图层设置为发生撤销操作的位置。
* **允许非线性历史记录**：请参阅[撤销历史记录](./undo-history.md#non-linear-undo-history)部分。
* **显示撤销工具提示**：在窗口右下角显示撤销工具提示。

## 主题

* **字体**：设置程序字体。
* **小字体**：设置程序的小字体。

## 扩展

* **添加扩展**：打开一个文件对话框，选择要添加的扩展（一个 `.zip` 或 `.aseprite-extension` 文件）。
* **禁用**：禁用选中的扩展。
* **卸载**：卸载选中的扩展。
* **打开文件夹**：在安装所选扩展的文件夹中打开原生文件资源管理器（Windows 文件资源管理器或 macOS Finder）。

## Aseprite 格式

* **Cel 内容格式**：更改 cel 的图像格式。请参阅 [*cel-chunk-0x2005*](https://github.com/aseprite/aseprite/blob/main/docs/ase-file-specs.md#cel-chunk-0x2005) 。

## 实验性

这些功能是实验性的，将来可能会被移除或更改，因此这里不列出。

## 重置

重置部分包含各种要重置的编辑器值。

要重置某个值，请选中它，然后按*重置*按钮。

---

**另请参阅**

[首选项文件夹](./preferences-folder.md) |
[重置首选项](./reset-preferences.md) |
[键盘快捷键](./keyboard-shortcuts.md) |
[自定义](./customization.md)
