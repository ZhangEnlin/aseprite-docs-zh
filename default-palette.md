# 默认调色板

新建精灵时会使用默认调色板。初始默认调色板是 [Richard "DawnBringer" Fhager 的 32 色调色板](http://pixeljoint.com/forum/forum_posts.asp?TID=16247)。

不过，你可以使用 ☰ *> 将调色板另存为默认值* 菜单选项来更改它：

![将调色板另存为默认值演示](./default-palette/save-as-default.gif)

---

**另请参阅**

[新建精灵](./new-sprite.md)